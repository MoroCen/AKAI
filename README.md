# Category-C
The purpose of this project is for practical Examination for Set-C

If Python is not installed
Run this in the Codespaces terminal:

# sudo apt update sudo apt install python3 -y

Check if Python is installed:
python3 --version

Install SQLITE3
# sudo apt update sudo apt install sqlite3 -y

Check SQLite3
# sqlite3 --version

Structured Data ETL Practical Exam Topic: Load structured CSV data into SQLite3 using Python

Student Task: Complete only the SQLite3 parts:

CREATE TABLE
INSERT INTO
SELECT FROM
The extraction and transformation code is already provided.

"""
Weather API-Based ELT Practical Exam

Process:
Ready Weather API → Extract → Load Raw JSON → Transform → Select

Cities:
1. Manila
2. Cebu

Student Task:
Complete only the SQLite3 parts:

1. CREATE TABLE
2. INSERT INTO
3. SELECT FROM

The program will:
- Extract weather data from a ready API.
- Store the raw JSON response in SQLite3.
- Transform the raw JSON into structured weather records.
- Display the transformed result.
"""